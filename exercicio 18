# Etapa 1 - Solicita ao usuário três valores distintos
n1 = float(input("Digite o primeiro valor: "))
n2 = float(input("Digite o segundo valor: "))
n3 = float(input("Digite o terceiro valor: "))

# Etapa 2 - Determina os dois maiores valores e calcula a soma
menor = min(n1, n2, n3)
soma = (n1 + n2 + n3) - menor

# Exibe o resultado
print(f"A soma dos dois maiores valores é: {soma}")
